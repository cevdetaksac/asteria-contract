# Disaster recovery — single contract

> **SoT** **≥ 1.4.65**

IR commands: `create_user`, `remote_logon`, `set_autologon` / `clear_autologon`,
`reboot`. HMAC + cloud confirm. Do not disable/logoff the signed-in operator.
